const FILLSCREEN = {
  width: '100%',
  height: '100%',
};

const HIDDEN = {
  width: 0,
  height: 0,
};

const ERRORTEXT = {
  textAlign: 'center',
  fontSize: 30,
  color: '#FFF',
};

const CENTER = {
  width: '100%',
  height: '100%',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  marginTop: -100,
};

const BOLD = {
  fontWeight: 'bold',
};

const HEADER = {
  width: '100%',
  height: 100,
  marginLeft: 20,
};

const HIGHLIGHT = {
  position: 'absolute',
  top: 90,
  left: 20,
  width: 430,
  height: 240,
  borderWidth: 5,
  borderRadius: 5,
};

const INFO = {
  width: '100%',
  height: 225,
  marginLeft: 30,
};

const INFO_TITLE = {
  lineHeight: 100,
  fontSize: 30,
  fontWeight: 'bold',
};

const INFO_DESCRIPTION = {
  fontSize: 25,
};

const LOGO = {
  width: 400,
  height: 100,
  resizeMode: 'contain',
};

const PLAYLIST = {
  marginLeft: 30,
};

const PLAYLIST_TEXT = {
  fontSize: 35,
  lineHeight: 100,
  fontWeight: 'bold',
};

const THUMBNAIL = {
  width: 420,
  height: 230,
};

const THUMBNAIL_IMAGE = {
  width: 410,
  height: 220,
};

export {
  BOLD,
  CENTER,
  ERRORTEXT,
  FILLSCREEN,
  HEADER,
  HIGHLIGHT,
  HIDDEN,
  INFO,
  INFO_DESCRIPTION,
  INFO_TITLE,
  LOGO,
  PLAYLIST,
  PLAYLIST_TEXT,
  THUMBNAIL,
  THUMBNAIL_IMAGE,
};
