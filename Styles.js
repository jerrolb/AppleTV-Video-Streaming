const VIDEO = {
  margin: 100,
  width: 500,
  height: 500
}

const BODY = {
  backgroundColor: '#000',
  height: '100%',
  width: '100%'
};

const ERRORTEXT = {
  // position: 'absolute',
  textAlign: 'center',
  fontSize: 30,
  color: '#FFF',
  margin: 200
}

const FULLSCREEN = {
    width: '100%',
    height: '100%'
};

export {
    BODY,
    ERRORTEXT,
    FULLSCREEN,
    VIDEO
};